# Public methodological prompt

Act as a measurement instrument. Code each supplied obligation-provision pair independently using only the supplied texts and rules. Decide the link status before the correspondence role and atomic institutional observables. Lexical overlap alone is insufficient. Cite the shortest exact supplied span needed for each affirmative or uncertain code. Return only schema-valid structured data. Do not use outside legal facts, expected results, earlier classifications, or manuscript claims.
